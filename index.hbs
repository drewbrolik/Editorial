{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}


{{!-- Featured Post --}}
{{#is "home"}}
{{#get "posts" filter="featured:true" limit="1" as |featured_post|}}
    {{#foreach featured_post}}
        <article id="banner">
            <div class="content">
                <header>
                    <h1><a href="{{url}}">{{title}}</a></h1>
                    {{#if custom_excerpt}}<p>{{custom_excerpt}}</p>{{/if}}
                </header>
                {{content words="42"}}
                <ul class="actions">
                    <li><a href="{{url}}" class="button big">Learn More</a></li>
                </ul>
            </div>
            {{#if feature_image}}
            <a href="{{url}}" class="image object">
                <img src="{{img_url feature_image}}" alt="{{title}}" />
            </a>
            {{/if}}
        </article>
    {{/foreach}}
{{/get}}
{{/is}}

{{!-- No Code Posts --}}
<section>

    <header class="major">
        <h2>Recent No Code</h2>
    </header>

    <section class="posts">
      {{#get "posts" filter="tag:no-code" limit="3" as |nocode_post|}}
        {{#foreach nocode_post}}
        <article>
            {{#if feature_image}}
            <a href="{{url}}" class="image fit"><img src="{{img_url feature_image}}" alt="{{title}}" /></a>
            {{/if}}
            <h3><a href="{{url}}">{{title}}</a></h3>
            <p>{{excerpt words="33"}}</p>
            <ul class="actions special">
                <li><a href="{{url}}" class="button">Full Story</a></li>
            </ul>
        </article>
        {{/foreach}}
      {{/get}}
    </section>

</section>

{{!-- Marketing Posts --}}
<section>

    <header class="major">
        <h2>Recent Marketing</h2>
    </header>

    <section class="posts">
      {{#get "posts" filter="tag:marketing" limit="3" as |marketing_post|}}
        {{#foreach marketing_post}}
        <article>
            {{#if feature_image}}
            <a href="{{url}}" class="image fit"><img src="{{img_url feature_image}}" alt="{{title}}" /></a>
            {{/if}}
            <h3><a href="{{url}}">{{title}}</a></h3>
            <p>{{excerpt words="33"}}</p>
            <ul class="actions special">
                <li><a href="{{url}}" class="button">Full Story</a></li>
            </ul>
        </article>
        {{/foreach}}
      {{/get}}
    </section>

</section>

{{!-- Posts --}}
{{!-- <section>

    <header class="major">
        <h2>Recent posts</h2>
    </header>

    <section class="posts">
        {{#foreach posts}}
        <article>
            {{#if feature_image}}
            <a href="{{url}}" class="image fit"><img src="{{img_url feature_image}}" alt="{{title}}" /></a>
            {{/if}}
            <h3><a href="{{url}}">{{title}}</a></h3>
            <p>{{excerpt words="33"}}</p>
            <ul class="actions special">
                <li><a href="{{url}}" class="button">Full Story</a></li>
            </ul>
        </article>
        {{/foreach}}
    </section>

</section> --}}

{{!-- Pagination --}}
{{!--
{{pagination}}
--}}

